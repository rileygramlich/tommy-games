{
  "name": "tommy-games",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tommy Games — a small, growing collection of card games you can play in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server/index.js",
    "dict": "node scripts/build-dictionary.mjs",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.57.0",
    "vite": "^7.1.14"
  },
  "engines": { "node": ">=20" }
}
